version: '3'

services:
  web:
    build: .
    command: bash -c "source .env && python manage.py makemigrations && python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    container_name: zi_service
    volumes:
      - .:/zi_service
    ports:
      - "8000:8000"